name: Deploy to erp.vexasoft.net

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: SSH and deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /var/www/erp.vexasoft.net
            git pull origin main
            php8.5 /usr/local/bin/composer install --no-dev --optimize-autoloader
            npm install
            npm run build
            php8.5 artisan migrate --force
            php8.5 artisan config:cache
            php8.5 artisan route:cache
            php8.5 artisan view:cache
            chown -R www-data:www-data storage bootstrap/cache
            systemctl restart php8.5-fpm
